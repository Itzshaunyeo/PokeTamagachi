const test=require('node:test');const assert=require('node:assert/strict');const generated=require('../src/move-data.generated.js');const battle=require('../src/battle-data.js');
test('bundled Black/White data contains form-specific official level-up learnsets',()=>{assert.equal(generated.versionGroup,'black-white');assert.ok(Object.keys(generated.learnsets).length>=150);assert.ok(Object.keys(generated.moves).length>=400);assert.ok(battle.learnsetFor('electric',25).some(lesson=>lesson.move==='quick-attack'));assert.ok(battle.learnsetFor('fire',6).some(lesson=>lesson.move==='flare-blitz'))});

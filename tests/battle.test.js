const test=require('node:test');const assert=require('node:assert/strict');const {resolveBattle}=require('../lan-battle.cjs');
test('LAN battle resolves one round and accepts a different move next turn',()=>{const defender={name:'Host',species:'Bulbasaur',level:5,speed:12,moveSet:[{name:'Tackle',power:40}]},challenger={name:'Guest',species:'Pikachu',level:8,speed:30,move:{name:'Spark',power:60}};const first=resolveBattle(defender,challenger);assert.equal(first.finished,false);assert.equal(first.first,'challenger');assert.equal(first.turns.length,2);assert.match(first.turns[0].text,/Turn 1 · Pikachu used Spark/);const second=resolveBattle(defender,{...challenger,move:{name:'Thunder',power:100},battleState:{challengerHp:first.finalHp.challenger,defenderHp:first.finalHp.defender,round:2}});assert.match(second.turns[0].text,/Turn 2 · Pikachu used Thunder/);assert.ok(second.finalHp.defender<first.finalHp.defender)});

test('a one-hit knockout ends before the opponent can respond',()=>{const result=resolveBattle({species:'Caterpie',level:1,speed:1},{species:'Charizard',level:50,speed:100,move:{name:'Flamethrower',power:100}});assert.equal(result.turns.length,1);assert.equal(result.turns[0].remainingHp,0);assert.equal(result.finished,true);assert.equal(result.won,true)});

const test=require('node:test');const assert=require('node:assert/strict');const {resolveBattle}=require('../lan-battle.cjs');
test('LAN battle resolution returns speed-ordered turns and deterministic damage',()=>{const result=resolveBattle({name:'Host',species:'Bulbasaur',level:5,speed:12},{name:'Guest',species:'Pikachu',level:8,speed:30,move:{name:'Spark',power:60}});assert.equal(result.won,true);assert.equal(result.first,'challenger');assert.equal(result.turns[0].actor,'challenger');assert.match(result.turns[0].text,/Pikachu used Spark/);assert.ok(result.exp>0);assert.ok(result.attack>result.counter)});
